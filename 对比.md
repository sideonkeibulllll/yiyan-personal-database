# 规划 vs 现状对比分析

> 生成时间：2026-07-22
> 对比基准：原始规划文档 v0.1 草案 vs 实际代码实现

---

## 总览

| 维度 | 规划 | 现状 | 完成度 |
|------|------|------|--------|
| 套件总数 | 9 大套件 | 9 大套件均有代码 | ~70% |
| 页面路由 | 5 页面 | 9 页面（含数据管理器、导出、编辑等） | 超出预期 |
| 技术栈 | React+Vite+Capacitor+SQLite+Zustand | 完全一致 | 100% |
| UI 组件库 | Ant Design Mobile 5 | **未使用**，改为纯手写 SVG + CSS | 偏离 |
| 交付阶段 | 6 Phase | Phase 1-3 基本完成，Phase 4-6 部分 | ~50% |

---

## 逐套件对比

### 套件 1：录入套件 `InputKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| 极速输入框 + 粘贴/发送 | ✅ | `HomePage.tsx` 实现完整 | ✅ 完成 |
| 粘贴自动填入剪贴板 | ✅ | `Clipboard.read()` 实现 | ✅ 完成 |
| 发送后底部轻提示 | ✅ | Toast 实现"已入库" | ✅ 完成 |
| 发送后切换"添加标签"/"添加信息" | ✅ | `mode` 状态切换实现 | ✅ 完成 |
| 3秒自动回退输入框 | ✅ | `modeTimerRef` 实现 | ✅ 完成 |
| 标签选择栏调出 | ✅ | `TagSelector` 弹层实现 | ✅ 完成 |
| AI 辅助标签建议 | ✅ | `TagSelector` 有 `showAISuggestion` prop 但 **未接入实际 AI 调用** | ⚠️ 半完成 |
| 信息附加面板（来源/组/补充） | ✅ | `InfoAdder` 组件存在，但首页"添加信息"跳转到 `EntryEditPage` 而非使用 `InfoAdder` | ⚠️ 偏离 |
| 来源预设选择 | ✅ | `InfoAdder` 有 `presetSources` localStorage | ⚠️ 组件存在但未被首页使用 |

### 套件 2：随机浏览套件 `RandomBrowser`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| 加权随机算法 | ✅ 星标×2 + 5天内使用×2 | `random.ts` 实现完全一致 | ✅ 完成 |
| 范围筛选（标签+星标） | ✅ | `filterEntries` + 筛选面板实现 | ✅ 完成 |
| 卡片堆叠展示 | ✅ | `cards-stack` 从上到下排列 | ✅ 完成 |
| 单击复制 | ✅ | `handleCopy` + `markAsUsed` | ✅ 完成 |
| 长按快捷菜单 | ✅ | 500ms 长按计时器 + `QuickMenu` bottom sheet | ✅ 完成 |
| 快捷菜单：编辑标签 | ✅ | 调出 `TagSelector` | ✅ 完成 |
| 快捷菜单：切换星标 | ✅ | `onToggleStar` | ✅ 完成 |
| 快捷菜单：详情显示 | ✅ | `panel='detail'` 面板 | ✅ 完成 |
| 快捷菜单：添加到组 | ✅ | `panel='group'` 组选择器 | ✅ 完成 |
| 快捷菜单：查看连线 | ✅ | 跳转 `/links/:entryId` | ✅ 完成 |
| 快捷菜单：AI 对话 | ✅ | `QuickMenu` 有"AI 对话"按钮但 `action: () => {}` 空函数 | ❌ 未实现 |
| 每屏卡片数配置 | ✅ | `settings.random.cardsPerPage` | ✅ 完成 |
| 自适应截取保留整数 | ✅ | 按屏幕排列，超出截取 | ✅ 完成 |
| 下一屏刷新 | ✅ | "刷新下一屏"按钮 | ✅ 完成 |
| 左右滑动切换 | 规划未明确 | 未实现 | — |

### 套件 3：标签套件 `TagKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| 手动添加标签 | ✅ | `TagSelector` 新建标签功能 | ✅ 完成 |
| 一条多标签 | ✅ | `entry_tags` 关联表 | ✅ 完成 |
| AI 建议标签（3-5个推荐） | ✅ | `ai.suggestTags()` 服务已写，但 **TagSelector 的 AI 按钮未接入** | ⚠️ 半完成 |
| 标签管理（合并） | ✅ | `TagManagePage` 只有重命名和删除，**无合并功能** | ❌ 缺失 |
| 标签管理（重命名） | ✅ | `renameTag` 实现 | ✅ 完成 |
| 标签管理（删除） | ✅ | `deleteTag` + 确认弹窗 | ✅ 完成 |
| 标签选择栏复用组件 | ✅ | `TagSelector` 在多页面复用 | ✅ 完成 |

### 套件 4：星标套件 `StarKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| 二元状态切换 | ✅ | `toggleStar` 实现 | ✅ 完成 |
| 独立于标签系统 | ✅ | `is_starred` 独立字段 | ✅ 完成 |
| 搜索/随机/对话中可筛选 | ✅ | 搜索和随机均支持星标筛选 | ✅ 完成 |
| 影响随机权重（×2） | ✅ | `calculateWeight` 实现 | ✅ 完成 |

### 套件 5：搜索套件 `SearchKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| 全文搜索 | ✅ | `LIKE '%keyword%'` | ✅ 完成 |
| 标签筛选 | ✅ | 点击标签 chip 筛选 | ✅ 完成 |
| 星标状态筛选 | ✅ | 三态切换（全部/已星标/未星标） | ✅ 完成 |
| 组合条件 | ✅ | 关键词+标签+星标可组合 | ✅ 完成 |
| 单击结果一键复制 | ✅ | `handleCopy` | ✅ 完成 |
| 长按结果快捷菜单 | ✅ | **未实现**，搜索结果无长按菜单 | ❌ 缺失 |
| 存为智能标签 | ✅ | **未实现** | ❌ 缺失 |

### 套件 6：连线套件 `LinkKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| 手动连线 | ✅ | 数据层 `createLink` 已实现，但 **UI 上无"关联到…"入口** | ⚠️ 半完成 |
| AI 关系建议 | ✅ | `ai.suggestRelation()` 服务已写，但 **UI 未接入** | ⚠️ 半完成 |
| 连线展示（相关条目） | ✅ | `LinkPage.tsx` 实现 | ✅ 完成 |
| 连线页面（随机排列） | ✅ | `LinkPage` 列表展示 | ✅ 完成 |
| 双向关联 | ✅ | `getLinksByEntryId` 查双向 | ✅ 完成 |

### 套件 7：AI 对话套件 `AIChatKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| 标签建议 | ✅ | `ai.suggestTags()` 已写，UI 未接入 | ⚠️ 半完成 |
| 自动关联建议 | ✅ | `ai.suggestRelation()` 已写，UI 未接入 | ⚠️ 半完成 |
| 围绕条目对话 | ✅ | `AIChatPanel` 组件完整实现 | ✅ 完成 |
| 对话中引用其他条目 | ✅ | **未实现** | ❌ 缺失 |
| 主动推送（录入后提醒） | ✅ | **未实现** | ❌ 缺失 |
| 浏览时轻提示 | ✅ | **未实现** | ❌ 缺失 |
| 长期记忆文件 | ✅ | 设置中有 `enableLongTermMemory` 开关，但 **无实际 `长时记忆.md` 文件管理** | ⚠️ 半完成 |
| AI 对话入口 | ✅ | `QuickMenu` "AI 对话"按钮空函数，`AIChatPanel` 组件存在但 **未被任何页面调用** | ❌ 未接入 |

### 套件 8：设置套件 `SettingsKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| AI API 配置（Key/模型/地址） | ✅ | `SettingsPage` 完整实现 | ✅ 完成 |
| DeepSeek 专属选项 | ✅ | temperature/maxTokens 配置 | ✅ 完成 |
| 自定义提示词 | ✅ | 类型定义有 `PromptConfig`，但 **设置页无提示词编辑 UI** | ❌ 缺失 |
| 上下文范围（近期窗口） | ✅ | `recentWindow` 配置存在，但 **设置页无 UI** | ❌ 缺失 |
| 上下文范围（标签上下文） | ✅ | `tagContext` 配置存在，但 **设置页无 UI** | ❌ 缺失 |
| 上下文范围（长效记忆） | ✅ | `enableLongTermMemory` 开关存在，但 **设置页无 UI** | ❌ 缺失 |
| 主动推送开关 | ✅ | `push.enabled` 配置存在，但 **设置页无 UI** | ❌ 缺失 |
| 相似度阈值 | ✅ | `similarityThreshold` 配置存在，但 **设置页无 UI** | ❌ 缺失 |
| 数据存储位置 | ✅ | Capacitor SQLite 配置 | ✅ 完成 |
| 备份/恢复 | ✅ | 导出功能完整，导入功能完整 | ✅ 完成 |
| 随机浏览配置 | 规划未明确 | 新增每屏卡片数配置 | ✅ 超出 |

### 套件 9：数据层套件 `DataKit`

| 功能点 | 规划 | 现状 | 状态 |
|--------|------|------|------|
| SQLite 建表 | ✅ | 6 张表完整建表 | ✅ 完成 |
| CRUD 操作 | ✅ | `IDatabaseService` 接口 + 双实现 | ✅ 完成 |
| Web 平台降级 | ✅ | `WebDatabaseService` (localStorage) | ✅ 完成 |
| 原生 SQLite | ✅ | `NativeDatabaseService` | ✅ 完成 |
| JSON 导出 | ✅ | `exportData` 支持 JSON | ✅ 完成 |
| Markdown 导出 | ✅ | `exportAsMarkdown` | ✅ 完成 |
| 选择性导出 | ✅ | 全量/星标/按标签 | ✅ 完成 |
| 增量导入 | ✅ | `incrementalImport` content hash 去重 | ✅ 完成 |
| Cloudflare D1 同步 | ✅ 后期 | 未实现 | — (后期) |
| 数据管理器 | 规划未明确 | `DataManagerPage` MT管理器风格双栏 | ✅ 超出 |

---

## 规划外新增功能

| 功能 | 说明 |
|------|------|
| 数据管理器 `DataManagerPage` | MT管理器风格双栏文件管理，支持标签/组/数据三种模式切换、跨窗口复制/移动、多选、导航历史 |
| 条目编辑页 `EntryEditPage` | 独立的全字段编辑页面（内容/来源/补充/标签/组/星标） |
| 导出页面 `ExportPage` | 独立导出页面，格式+范围选择 |
| 随机浏览配置 | 每屏卡片数可配置 |

---

## 架构偏离

| 项目 | 规划 | 现状 | 影响 |
|------|------|------|------|
| UI 组件库 | Ant Design Mobile 5 | 纯手写 SVG + CSS (glass morphism) | 无第三方依赖，但开发效率降低，风格一致性靠自律 |
| 首页"添加信息" | 应使用 `InfoAdder` 组件 | 跳转到 `EntryEditPage` | 体验偏离，InfoAdder 组件未被使用 |
| `InfoAdder` 组件 | 应在首页使用 | 孤立组件，未被任何页面引用 | 死代码 |
| `AIChatPanel` 组件 | 应在随机浏览长按菜单调用 | 孤立组件，未被任何页面调用 | 死代码 |
| 设置页提示词编辑 | 规划有 PromptConfig UI | 缺失 | 用户无法自定义 AI 提示词 |
| 上下文范围设置 UI | 规划有三种上下文配置 | 缺失 | AI 对话上下文不可调 |

---

## 完成度汇总

| 阶段 | 规划内容 | 完成度 |
|------|----------|--------|
| Phase 1 | 录入+随机+搜索+数据层 | ✅ ~95% |
| Phase 2 | 标签+星标+权重+快捷菜单 | ✅ ~90% |
| Phase 3 | 信息附加+组合筛选+导出 | ⚠️ ~80%（智能标签缺失） |
| Phase 4 | AI 标签建议+对话面板 | ⚠️ ~40%（服务层写好，UI 未接入） |
| Phase 5 | 连线+AI关系建议 | ⚠️ ~50%（数据层完成，手动连线UI缺失） |
| Phase 6 | 主动推送+长期记忆+D1 | ❌ ~10%（仅配置字段存在） |

**总体完成度：约 55-60%**

---

## 关键缺口清单

1. **AI 对话入口未接入** — `AIChatPanel` 组件写好了但没人调用
2. **AI 标签建议未接入** — `ai.suggestTags()` 写好了但 TagSelector 的 AI 按钮是空操作
3. **AI 关系建议未接入** — `ai.suggestRelation()` 写好了但无 UI 入口
4. **手动连线 UI 缺失** — 数据层支持，但无"关联到…"搜索选择入口
5. **搜索结果长按菜单缺失** — 规划说同随机浏览菜单
6. **智能标签（存搜索条件）缺失**
7. **标签合并功能缺失**
8. **设置页缺少：提示词编辑、上下文范围、主动推送开关**
9. **长期记忆文件管理缺失**
10. **主动推送功能缺失**
11. **Ant Design Mobile 未使用** — 纯手写 UI
12. **InfoAdder 组件孤立** — 首页未使用

---

*本文档由天喵自动生成，用于项目进度审查与下一步规划参考 (｀・ω・´)*
